import 'areacalculator.dart';

class VolumeCalculator extends AreaCalculator {
  VolumeCalculator(List shapes) : super(shapes);

  sum() {
    // logic to calculate the volumes and then return num
    // return $summedData;
  }
}
